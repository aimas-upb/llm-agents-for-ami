apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "time-telling-agent-chart.fullname" . }}-test-connection"
  labels:
    {{- include "time-telling-agent-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "time-telling-agent-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
