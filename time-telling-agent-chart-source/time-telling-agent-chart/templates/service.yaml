apiVersion: v1
kind: Service
metadata:
  name: {{ include "time-telling-agent-chart.fullname" . }}
  labels:
    {{- include "time-telling-agent-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "time-telling-agent-chart.selectorLabels" . | nindent 4 }}
